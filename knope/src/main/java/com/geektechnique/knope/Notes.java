package com.geektechnique.knope;

public class Notes {
    // need to redesign
    // make service soley to get which video needs to rendered, nothing else.
    // run current 0.0.2 in meantime
    // make new service that calls this one, runs colab renderer based on result

    //why? because this fails when trying to run headless
}
